# v1.2 Release Notes

## Pilot Operations & Go-Live Controls

v1.2 adds an explicit pilot-readiness gate model and operational guidance for backup/restore, data governance, malware quarantine, incident response, worker isolation, tenant isolation, and university sign-off.

The gate model is deliberately fail-closed: a fresh environment cannot be considered ready until every required gate is explicitly passed or formally waived.
